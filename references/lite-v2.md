# Legacy OpenCode Lite v2 reference

This filename is retained for existing links. New skill instructions and documentation use
[OpenCode engine settings and limits](opencode.md).

The runtime implementation remains in `scripts/lite.py` for import compatibility, while
`scripts/worker.py` is the canonical CLI entrypoint.
