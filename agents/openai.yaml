interface:
  display_name: "OpenCode Worker"
  short_description: "Delegate implementation and testing to OpenCode"
  default_prompt: "Use $opencode-worker to implement this task, run the needed tests, and check the result."
policy:
  allow_implicit_invocation: true
