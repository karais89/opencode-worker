interface:
  display_name: "OpenCode Worker"
  short_description: "Delegate coding, testing and code investigation"
  default_prompt: "Use $opencode-worker to implement or investigate this task, validate within its authorized scope, and check the result."
policy:
  allow_implicit_invocation: true
