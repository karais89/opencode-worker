interface:
  display_name: "OpenCode 작업 위임"
  short_description: "작업을 한 세션에 위임하고 결과와 검증 근거를 확인합니다"
  default_prompt: "$opencode-worker로 이 저장소 작업을 설정된 OpenCode Worker 한 세션에 맡기고, 압축 결과와 구체적인 미확인 사항만 점검해 주세요."
policy:
  allow_implicit_invocation: true
