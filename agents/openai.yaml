interface:
  display_name: "OpenCode Worker Lite v2"
  short_description: "One OpenCode worker with pinned routing and compact evidence"
  default_prompt: "Use $opencode-worker to hand this repository task to one configured OpenCode Worker, then check only the compact result and concrete gaps."
policy:
  allow_implicit_invocation: true
